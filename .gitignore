# macOS
.DS_Store

# Xcode — per-user state
xcuserdata/
*.xcuserstate

# Build output
build/
DerivedData/
Packages/**/.build/

# Notarized release artifact — CI publishes release zips; don't track a local one
Amanuensis.zip

# Git worktrees
.worktrees/

# Brainstorming visual companion (per-session mockups)
.superpowers/

# Local Claude Code state — not vendored (skills, settings, daemon config)
.claude/
.xcodebuildmcp/

# Local Hammerspoon build daemon (machine-specific)
build-daemon.lua

# Machine-local Claude instructions (sandbox / daemon specifics)
CLAUDE.local.md

# Outside-sandbox daemon helper scripts (only meaningful with the local daemon)
scripts/xcode-build-helper.sh
scripts/log-helper.sh

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    # macos-26: Xcode 26 test bundles link libswift_DarwinFoundation2.dylib, a
    # macOS 26 runtime lib absent on the macos-15 (Sequoia) image, so `swift test`
    # fails to dlopen the test bundle there. The app targets macOS 26.3 anyway.
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode 26
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26'

      - name: Cache SwiftPM
        uses: actions/cache@v4
        with:
          path: ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: spm-

      # The app-hosted AmanuensisTests target needs a real audio device, the TCC
      # private SPI, and a running NSApp — none of which exist on a headless
      # runner. CI verifies the deterministic SPM core here and that the app
      # compiles below; it does NOT exercise app behavior.
      - name: Unit tests (AudioPipeline core)
        run: swift test --package-path Packages/AudioPipeline

      - name: Build app (Release, ad-hoc)
        run: |
          xcodebuild build \
            -project Amanuensis.xcodeproj -scheme Amanuensis \
            -configuration Release -destination 'platform=macOS' \
            CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO

      # The step above builds only the runner's native (arm64) slice; nothing on a
      # PR compiled x86 until now. Local-model code is arm64-only + runtime-gated off
      # on Intel, so an x86-only compile break (Float16, strict-concurrency sends)
      # could slip through every arm64 PR and only detonate in the tag-triggered
      # release.yml x86 build. Compile the Intel slice here so PRs catch it.
      - name: Build app (Release, x86_64 — catch Intel-only breaks)
        run: |
          xcodebuild build \
            -project Amanuensis.xcodeproj -scheme Amanuensis \
            -configuration Release -destination 'generic/platform=macOS' \
            ARCHS=x86_64 ONLY_ACTIVE_ARCH=NO \
            CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO
